# 前端启动说明

基于 Vue 3 + TypeScript 搭建。使用 Vite 作为构建工具。预置依赖库包括 `vue-router`, `axios`, `pinia`。

## 如何运行
在当前目录 `/frontend/` 下：
1. `npm install` 安装所有必须的依赖。
2. `npm run dev` 运行前端开发服务器。默认通过配置代理请求至 `localhost:8080/api`。
3. 若需打包请使用 `npm run build`。
